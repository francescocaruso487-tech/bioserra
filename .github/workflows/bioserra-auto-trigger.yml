name: BioSerra - Auto Trigger Cervelli AI

# ⚠️ DISABILITATO — N8N dismesso (migrazione a GitHub Actions completata).
# Gli endpoint francesco467.app.n8n.cloud non sono più attivi.
# Lasciato solo workflow_dispatch manuale; trigger push e chiamate curl rimossi.

on:
  workflow_dispatch:

jobs:
  trigger-cervelli:
    runs-on: ubuntu-latest
    if: false   # blocca l'esecuzione anche se avviato manualmente per errore
    steps:
      - name: Disabilitato
        run: echo "Workflow N8N disabilitato — pipeline ora interamente su GitHub Actions"
